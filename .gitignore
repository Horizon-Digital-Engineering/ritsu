node_modules/
dist/
# Runtime data — DB, WAL/SHM, .admin-token, .master-key, etc. Ignore the
# whole dir; keep only the placeholder so the path exists on a fresh clone.
data/*
!data/.gitkeep
.env
*.log
.litellm-venv/
coverage/
.c8_output/
